{
  "name": "@klockn/ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@klockn/shared": "*",
    // Anthropic SDK — powers availability optimization and venue suggestions
    "@anthropic-ai/sdk": "^0.24.0",
    // Internal API server — called by the backend job queue
    "express": "^4.19.0",
    "date-fns": "^3.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "tsx": "^4.10.0",
    "vitest": "^1.6.0"
  }
}
