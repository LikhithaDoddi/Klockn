// Landing page — public-facing, no auth required
// Redirects authenticated organizers to /dashboard

export default function HomePage() {
  return (
    <main>
      <h1>Klockn</h1>
      <p>Find the date that maximizes attendance — before you publish.</p>
      {/* TODO: Build marketing landing page */}
    </main>
  )
}
