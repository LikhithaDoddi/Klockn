// Organizer dashboard — requires authentication
// Shows all events the organizer has created with their status

export default function DashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      {/* TODO: EventList component — lists events with status chips */}
      {/* TODO: "Create Event" CTA button */}
    </div>
  )
}
